apiVersion: v1
kind: Service
metadata:
  name: {{ include "gharts.fullname" . }}-backend
  labels:
    {{- include "gharts.backend.labels" . | nindent 4 }}
  {{- with .Values.backend.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gharts.backend.selectorLabels" . | nindent 4 }}

# Made with Bob
